# Define for list of numbers
numbers = [1, 2, 3, 4, 5]

# Initialize a variable to store the sum
sum_of_numbers = 0

# Use a for loop to iterate through the list
for num in numbers:
    sum_of_numbers += num

# Print the result
print(f"The sum of numbers is: {sum_of_numbers}")
