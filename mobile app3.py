# Sample list of numbers in the list
numbers = [5, 2, 8, 1, 9]

# Sort  list in the descending orders
sorted_numbers_desc = sorted(numbers, reverse=True)

# Print the sorted list in descending orders 
print(sorted_numbers_desc)
